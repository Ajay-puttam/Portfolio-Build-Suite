Build a Next.js 14 (App Router) + Tailwind CSS portfolio website foundation for a "Creative AI Technologist" personal brand. Do not generate any page content yet — only set up the design system, fonts, globals, layout, and navbar.

TECH STACK:
- Next.js 14 with App Router
- Tailwind CSS v3
- TypeScript
- Framer Motion (install it)
- next/font for fonts

FONTS:
- Heading font: "Syne" from Google Fonts (weights 400, 600, 800)
- Body font: "Inter" from Google Fonts (weights 300, 400, 500)
- Use next/font/google to load both

TAILWIND CONFIG (tailwind.config.ts):
Extend the theme with these exact custom values:
- colors:
  - brand: { DEFAULT: '#7C3AED', light: '#A78BFA', dim: '#4C1D95' }
  - surface: { DEFAULT: '#0A0A0F', card: '#111118', border: '#1E1E2E' }
  - text: { primary: '#F8F8FF', secondary: '#A0A0B8', muted: '#505068' }
- fontFamily:
  - display: ['Syne', 'sans-serif']
  - body: ['Inter', 'sans-serif']
- backgroundImage:
  - 'gradient-radial': 'radial-gradient(var(--tw-gradient-stops))'
  - 'noise': "url('/noise.png')"
- animation:
  - 'fade-up': 'fadeUp 0.6s ease forwards'
  - 'glow-pulse': 'glowPulse 3s ease-in-out infinite'
- keyframes for fadeUp and glowPulse

GLOBALS (globals.css):
- Background: #0A0A0F (near black)
- Default text: #F8F8FF
- Custom scrollbar: thin, brand purple track
- ::selection highlight: brand purple with white text
- Add CSS custom properties for all design tokens
- Smooth scroll behavior

ROOT LAYOUT (app/layout.tsx):
- Import both fonts, apply to html element
- Import globals.css
- Add metadata: title "Varun | Creative AI Technologist", description about AI-native creative builder

NAVBAR COMPONENT (components/Navbar.tsx):
- Fixed top navbar
- Left: stylised text logo "V." in Syne font, brand purple, large weight
- Right: nav links — Work, About, Contact — in Inter, text-secondary, hover transition to white
- Center: subtle pill badge saying "Open to work" with a green pulsing dot
- Background: transparent on top, blurs and gains dark bg on scroll (use useScrollPosition hook)
- Mobile: hamburger menu
- Smooth underline hover effect on links
- No border, just backdrop-blur on scroll

Give me:
1. tailwind.config.ts
2. app/globals.css
3. app/layout.tsx
4. components/Navbar.tsx
5. A useScrollPosition.ts hook

Make the code clean, typed, and production-ready.